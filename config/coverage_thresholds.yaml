# AMI-AGENTS Test Coverage Thresholds

unit:
  path: tests/unit
  min_coverage: 89
  source_path: ami
  runner: uv run python -m pytest

integration:
  path: tests/integration
  min_coverage: 50
  source_path: ami
  runner: uv run python -m pytest
