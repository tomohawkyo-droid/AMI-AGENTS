# Test Coverage Thresholds Configuration
# Used by verify_coverage.py to enforce minimum coverage levels
# TODO: Restore thresholds after adding more tests
# - Unit: 90% -> 20% (temporary - current coverage ~25%)
# - Integration: 75% -> 20% (temporary - current coverage ~25%)

unit:
  path: "tests/unit"
  min_coverage: 20
  source_path: "ami"

integration:
  path: "tests/integration"
  min_coverage: 20
  source_path: "ami"
