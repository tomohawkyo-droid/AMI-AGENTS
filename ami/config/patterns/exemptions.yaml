# Pattern Check Exemptions
# Used by CodeQualityValidator in agents/ami/hooks.py
#
# Files listed here are exempt from ALL fast pattern checks.
# Patterns can be:
# - File extensions (e.g. ".md", ".txt") - matches files ending with pattern
# - Path segments (e.g. "/tests/", "conftest.py") - matches if pattern appears anywhere in path

version: "1.0.0"

# Files exempt from pattern checks (contain error messages with forbidden patterns)
pattern_check_exemptions:
  - "agents/ami/hooks.py"
  - "agents/ami/validators.py"
  - "scripts/config/patterns/bash_commands.yaml"
  - "scripts/config/patterns/python_fast.yaml"
  - "scripts/config/patterns/exemptions.yaml"
  - "scripts/config/patterns/README.md"
  - "base/backend/workers/"  # Worker implementations are allowed to use subprocess
  - ".md"
  - ".txt"
  - "/tests/"
  - "conftest.py"
