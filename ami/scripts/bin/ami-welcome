#!/usr/bin/env bash
# ami-welcome: Print the AMI Orchestrator banner
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
AMI_ROOT="$("$SCRIPT_DIR/ami-pwd")"
export AMI_ROOT
exec bash "$AMI_ROOT/ami/scripts/shell/ami-banner.sh" "$@"
