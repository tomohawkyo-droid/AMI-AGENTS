extensions:
  - name: ami-gcloud
    binary: .gcloud/google-cloud-sdk/bin/gcloud
    description: Google Cloud CLI (gcloud SDK)
    category: dev
    features:
      - compute
      - storage
      - iam
      - config
    hidden: true
    bannerPriority: 220
    minVersion: "500.0.0"
    check:
      command: ["{binary}", "--version"]
      versionPattern: "Google Cloud SDK\\s+(\\d+\\.\\d+\\.\\d+)"
      healthExpect: "Google Cloud SDK"
      timeout: 5
