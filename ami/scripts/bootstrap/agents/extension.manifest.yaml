extensions:
  - name: ami-claude
    binary: .venv/node_modules/.bin/claude
    description: Claude Code AI assistant
    category: agents
    features:
      - -p
      - --continue
      - --resume
      - --allowedTools
    bannerPriority: 500
    check:
      command: ["{binary}", "--version"]
      versionPattern: "(\\d+\\.\\d+\\.\\d+)"
      healthExpect: "Claude Code"
      timeout: 5

  - name: ami-gemini
    binary: .venv/node_modules/.bin/gemini
    description: Gemini CLI AI assistant
    category: agents
    features:
      - -p
      - --resume
      - --yolo
      - mcp
      - extensions
    bannerPriority: 510
    check:
      command: ["{binary}", "--help"]
      healthExpect: "Gemini CLI"
      timeout: 5

  - name: ami-qwen
    binary: .venv/node_modules/.bin/qwen
    description: Qwen Code AI assistant
    category: agents
    features:
      - -p
      - --resume
      - --yolo
      - mcp
      - extensions
    bannerPriority: 520
    check:
      command: ["{binary}", "--help"]
      healthExpect: "Qwen Code"
      timeout: 5
